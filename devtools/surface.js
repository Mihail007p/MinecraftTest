// Считает, из чего состоит поверхность мира вокруг спавна.
require('./harness.js');
const D = loadGame();
let t = 1000; for (let i = 0; i < 60; i++) { t += 16; global.__RAF()(t); }
const NAMES = {0:'воздух',1:'трава',2:'земля',3:'КАМЕНЬ',4:'бревно',5:'доски',6:'листва',7:'песок',8:'вода',
 13:'верстак',29:'факел',30:'сундук',31:'печь',32:'песчаник',33:'стекло',34:'дверь',35:'лестница',36:'забор',
 37:'калитка',38:'кровать',39:'ковёр',40:'люк',41:'табличка',42:'плита',44:'кам.кирпич',45:'гл.камень',
 46:'костёр',47:'бочка',48:'коптильня',49:'ткацкий',50:'кузнечный',51:'точило',53:'стена',63:'нажим.плита',
 73:'УГОЛЬ.РУДА',74:'ЖЕЛ.РУДА',75:'ЗОЛ.РУДА',76:'АЛМ.РУДА',85:'ГЛУБИННЫЙ',86:'снег',87:'кактус',90:'лёд',
 91:'подзол',92:'глина',94:'мицелий',95:'терракота',96:'тыква',101:'грядка',102:'сено'};
const R = Number(process.argv[2] || 40);
const cnt = {}; let cols = 0; const spots = [];
for (let x = -R; x <= R; x++) for (let z = -R; z <= R; z++) {
  for (let y = D.CHUNK_HEIGHT - 1; y > 0; y--) {
    const b = D.getBlock(x, y, z);
    if (b !== 0 && b !== 8) {
      cnt[b] = (cnt[b] || 0) + 1; cols++;
      if ((b === 3 || b === 73 || b === 74 || b === 75 || b === 76 || b === 85) && spots.length < 10) spots.push([x, y, z, b]);
      break;
    }
  }
}
const rock = (cnt[3]||0)+(cnt[73]||0)+(cnt[74]||0)+(cnt[75]||0)+(cnt[76]||0)+(cnt[85]||0);
console.log('колонок:', cols, '| камень/руда сверху:', rock, '(' + (100*rock/cols).toFixed(2) + '%)');
console.log(Object.entries(cnt).sort((a,b)=>b[1]-a[1]).map(([k,v])=>(NAMES[k]||('id'+k))+':'+v).join('  '));
if (spots.length) console.log('примеры:', JSON.stringify(spots));
